{
  "name": "blog_list-backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Ronen",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env  NODE_ENV=test node --test",
    "build:ui": "rm -rf dist && cd ../blog_list-frontend && npm run build && cp -r dist ../blog_list-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "express-async-errors": "^3.1.1",
    "mongoose": "^8.15.1"
  },
  "devDependencies": {
    "supertest": "^7.1.1"
  }
}
